"""Document classification: TF-IDF + dense features, a calibrated logistic regression with an
abstain threshold, rule-based metadata extraction, a zero-shot LLM baseline and the
evaluation that compares them."""
