"""Extraction evaluation: per-field normalised matching, list matching by product name,
document-level correctness, bootstrap CIs, paired comparisons between strategies, and the
markdown/JSON report writer."""
