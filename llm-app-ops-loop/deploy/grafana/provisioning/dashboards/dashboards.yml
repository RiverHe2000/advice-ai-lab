apiVersion: 1
providers:
  - name: opsloop
    folder: LLM application operations
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
